#!/bin/bash
# JAZZ bootstrapper — downloads and runs setup.sh with a proper terminal
# Usage: curl -sL jauz256.github.io/jazz/s | bash
t=$(mktemp)
curl -fsSL https://raw.githubusercontent.com/Jauz256/jazz/main/setup.sh -o "$t" 2>/dev/null
bash "$t" "$@"
rm -f "$t"
